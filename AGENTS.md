# 城影记项目协作约定

## 开发与交付

- 默认用中文沟通，按 README.md 的导航和 docs/development-plan.md 推进；每次完成一个任务或紧密相关的任务组。
- 开始工作前检查 Git 状态并保留已有改动；实现后执行与变更范围相称的验证，并更新对应任务状态和交付记录。
- 用户已要求：以后每完成一个开发步骤，都要把代码和相关文档同步到 GitHub。这是持续授权，无需每次再询问是否提交或推送。
- “每一步”指可独立交付的开发任务、修复或文档更新，不要求每条命令后都创建提交。仅讨论或只读检查、没有文件改动时无需创建空提交。

## GitHub 同步

- 仓库：https://github.com/Staid-hhq/city-memories ，可见性为公开；当前主分支为 main，远程名为 origin。
- 每次交付前审查差异和暂存清单，完成必要验证，创建说明清楚的提交，再推送到相应远程分支。默认沿用 main；若当前任务明确使用工作分支或 Worktree，则按该任务的分支安排同步。
- 不把无关改动夹带进提交，不强制推送，不丢弃他人或用户改动；遇到远程更新时先检查并按正常 Git 流程处理。
- 推送后核对本地与远程分支提交一致，交付说明中给出同步结果和简短提交号。推送失败时保留本地提交，明确报告原因，不能宣称 GitHub 已更新。
- 提交使用 GitHub noreply 邮箱，避免公开个人邮箱；优先仅对当前仓库或当前命令配置，不修改全局 Git 身份。

## 公开仓库的数据边界

- 用户明确要求密钥不能上传。提交前检查实际差异和文件清单，排除真实 API Key、令牌、密码、会话数据及含这些内容的日志。
- 本地环境配置、私有数据库、原始照片、含私人内容的测试截图和运行数据留在本机；只提交使用占位符的配置示例。
- 遵守 .gitignore；若新增配置或数据路径，先补齐忽略规则。不要用强制添加绕过这些规则，不把凭据放入 Git 远程 URL。
- GitHub 同步仅指源码和文档版本管理；不代表发布网站，也不代替照片和数据库的独立备份。
