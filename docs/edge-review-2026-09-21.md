# Edge 新版原型关键复查

日期：2026-09-21。结论：本轮关键操作通过，未发现需要修改代码的问题。

环境：用户电脑上的 Microsoft Edge，已安装版本 153.0.4234.48；通过 Edge 浏览器扩展连接用户已打开的本机原型页，实际点击和拖动。不是 Codex 内置 Chromium 的替代验证。此前两次 Windows 电脑操作工具因无法识别网页地址而停止，不计为通过；本轮连接成功后完成以下检查，没有刷新或重新请求整张地图。

| 检查 | 实际结果 |
| --- | --- |
| 中国背景 | 页面报告 34/34 份几何已加载、缺失 0，地图完整显示本次接口取得的背景 |
| 城市入口与点亮 | 380 个城市及同级/特殊单位入口；默认仅深圳有 6 张照片，着色区域数量为 1；广州、香港空影集未点亮 |
| 最新视觉样式 | 城市浅暖橙填充 `#efb98e`、边线 `#b26f47` / 1.5 px；省界 `#c2cbb9` / 0.65 px，实际截图可见 |
| 缩放与拖移 | 点击放大至 1.45 倍，再实际拖动；地图变换同时保留缩放与位移 |
| 点击城市范围 | 实际点击深圳的着色区域，进入深圳年份影集页面 |
| 年份框 | 按 2026、2024、未标年份顺序显示，2024 年有 6 张照片 |
| 照片与原图 | 六张照片均完成解码；第一张原图为 4608 × 3456，查看层正常显示，前后切换照片正常 |
| 单张文字 | 输入临时测试文字、保存并切换照片后仍能读取 |
| 顺序与文字关联 | 将第一张后移，顺序变为 `24-3.jpg → 24-1.jpg`；第二张打开的仍是原来的 `24-1.jpg`，测试文字随照片保留 |
| 返回地图 | 返回后完整保留测试前的 1.45 倍缩放与拖动位移 |
| 搜索与空影集 | 搜索广州进入 2025 年空影集，显示 0 张照片及添加入口，城市仍未点亮 |
| 重复年份 | 再创建广州 2025 年时提示“这本影集已经有了，已为你打开。”；影集总数未增加 |
| 本轮日志 | 连接后采集到的浏览器错误、警告为空，不据此推断连接前的日志 |

收尾已将测试文字恢复为原值、照片顺序恢复为原顺序，清空城市搜索并返回中国全图；默认数据仍为 1 座点亮、5 本影集、6 张照片。保留用户的 Edge 原型页。

本轮没有修改应用代码，也未安装软件或修改源照片。未重复测试新账号注册、本地文件添加、照片拖动手柄、3000 张规模、网络失败恢复或真实账号和持久存储。地图首次加载速度、全部城市映射准确性、完整版图要素及正式上线条件继续跟踪；本轮 34/34 成功不等于地图所有要素已验收。

阶段 4 的新版 Edge 关键复查已补齐；阶段整体验收仍包含用户对页面与流程的审阅，不自动进入正式开发。
